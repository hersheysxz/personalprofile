{
  "name": "personalprofile-backend",
  "version": "1.0.0",
  "description": "Backend API for Rachel Regacho personal portfolio with contact form and MongoDB",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check routes/contact.js && node --check config/email.js && node --check models/Contact.js"
  },
  "keywords": ["portfolio", "contact-form", "mongodb", "express"],
  "author": "Rachel A. Regacho",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "dotenv": "^16.0.3",
    "nodemailer": "^9.0.3",
    "cors": "^2.8.5",
    "express-validator": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
